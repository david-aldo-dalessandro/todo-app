/* ItemsOrder.js
 * David D'Alessandro
 * March 26, 2024
 * Component for changing the order of the item in the list
 */

import { useDispatch } from "react-redux";

const ItemsOrder = ({ item }) => {
  return (
    <>
      <button>^</button>
      <button>v</button>
    </>
  );
};

export default ItemsOrder;
